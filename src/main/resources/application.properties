spring.application.name=PasswordHashingDemo

# Disable Thymeleaf caching for hot reloading during development
spring.thymeleaf.cache=false

# Enable live reload (optional but recommended with DevTools)
spring.devtools.livereload.enabled=true
